/* eslint-disable no-unused-vars */
import React from "react";
import "./Textarea.scss";

const Textarea = () => {
  return <textarea name="content" placeholder="Take a note..." rows="3" />;
};

export default Textarea;
